name: Java Maven Build

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - uses: actions/java@v1
      with:
        java-version: 11
    - name: Build Project
      run: mvn clean package install --file pom.xml
